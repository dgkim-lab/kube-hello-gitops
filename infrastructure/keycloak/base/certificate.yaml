apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: keycloak-tls
spec:
  secretName: keycloak-tls
  issuerRef:
    name: letsencrypt-route53-prod
    kind: ClusterIssuer
  dnsNames:
    - keycloak.k3s-test.dgkim.net
