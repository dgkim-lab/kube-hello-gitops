apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: kube-hello-app
spec:
  ingressClassName: traefik
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: kube-hello-app
                port:
                  number: 80
