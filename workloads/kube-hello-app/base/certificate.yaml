apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: kube-hello-app-tls
spec:
  secretName: kube-hello-app-tls
  issuerRef:
    name: letsencrypt-route53-prod
    kind: ClusterIssuer
  dnsNames:
    - hello.k3s-test.dgkim.net
